spring.application.name=BBTTResort

# ================= SERVER =================
server.port=4040

# ================= MYSQL =================
spring.datasource.url=jdbc:mysql://localhost:3306/bbtt_resort_db
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Jackson ? dates d?ng ISO string thay v� timestamp array
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false

# ================= FILE =================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ================= CLOUDINARY =================
cloudinary.cloud-name=bbtt-resort
cloudinary.api-key=237175291189391
cloudinary.api-secret=8FQppchaa9V9siq-Iwj83vrerks

# ================= EMAIL (Brevo HTTP API) =================
# Lý do dùng Brevo thay vì SMTP: Railway chặn kết nối ra ngoài tới smtp.gmail.com:587
# (đã xác nhận qua log "Connection timed out"), trong khi HTTPS:443 thì không bị chặn.
# Khoá API KHÔNG được hardcode — phải đặt biến môi trường BREVO_API_KEY trên Railway (tab Variables).
brevo.api.key=${BREVO_API_KEY:}
brevo.sender.email=${BREVO_SENDER_EMAIL:giabao27v@gmail.com}

# ================= VNPAY =================
# VNPay Config
vnpay.tmnCode=1C4BTBYS
vnpay.secretKey=O9S2K8IPAM0N4V8H60BFSEVZY1N044HG
vnpay.returnUrl=${VNPAY_RETURN_URL:http://localhost:3000/vnpay-return}
vnpay.vnpayUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.apiUrl=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
